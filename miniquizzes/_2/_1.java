package miniquizzes._2;

public class _1 {
    public static void main(String[] args) {
        int x = 8;
        int y = 5;
        boolean p = true;
        boolean q = false;

        // PART 1: What does each expression evaluate to? (true or false)

        // a) x > y && x < 10
        // b) y == 5 || p == false
        // c) !(x <= y)

        // PART 2: De Morgan's Law
        // Write an equivalent expression WITHOUT the outer NOT (!)

        // !(p && q)
        // !(x < 10 || y > 3)
    }
}
